[project]
name = "pid-plants"
version = "0.1.0"
description = "Plant testbed for pidkit validation against dynamic simulations"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.1",
    "pidkit",
]

[tool.uv.sources]
pidkit = { git = "https://github.com/chrisfox9158/pidkit.git", rev = "feature/autotune" }
